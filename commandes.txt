# ===== DOCKER (NOUVEAU - RECOMMANDÉ) =====
# Démarrer tout l'écosystème avec Docker
./docker-start.sh

# OU manuellement :
docker-compose up --build -d

# Voir les logs
./docker-logs.sh

# Arrêter et nettoyer
./docker-clean.sh

# Lancer le scraping avec Docker
docker-compose --profile scraping up scraper-service

# Lancer le pipeline de prédiction avec Docker
docker-compose --profile pipeline up prediction-pipeline

# ===== MÉTHODE CLASSIQUE (Ancienne) =====
# demarrer la base de données
docker-compose up -d mysql

# demarrer l'api crud
cd cinapps_api
uvicorn app.main:app --reload --port 8000

#demarrer l'api IA
cd API_s
uvicorn main:app --reload --port 8001

#lancer le scrapping
cd automatisation
scrapy crawl alloc_newfilms

#lancer les predictions
se mettre à la racine projet
lancer:
python3 prediction_pipeline.py

#lancer l'interface Streamlit
cd streamlit
streamlit run app.py --server.port 8501 

# lancer prometheus
cd monitoring
docker-compose up
http://localhost:9090
Clique sur “Status” > “Targets” pour voir l'état "up"
Puis cliquer sur graph et tapper predictions_total et on voit 18
verification possible des logs sur http://localhost:8000/metrics

#lancer grafana
http://localhost:3000
utilisateur: admin
motdepasse: admin
cliquer sur connections
ensuite sur add data source
choisir prometheus
dans le champ url mettre: http://prometheus:9090
tout en bas: save and test
ensuite aller dans dashboards (a gauche)
puis cliquer sur new -> new dashboards
puis cliquer sur add visualisation
prendre prometheus comme source

*************************************************************

# permet de lancer d'un coup les conteneurs
./docker-start.sh 

